**[← 返回第9章首頁](ch9-index.md)**

---

### 9.9 與其他模組的交互

#### 9.9.1 與Ch1（資料源註冊中心）交互

```python
# Ch9調用Ch1 API
GET /api/v1/data-sources              # 列出所有資料源
GET /api/v1/data-sources/{id}/status  # 獲取資料源狀態
POST /api/v1/data-sources/search      # 搜索資料源

# 通過事件驅動通信
Event: data-source.created
Event: data-source.updated
Event: data-source.deleted
```

#### 9.9.2 與Ch3（資料源健康監測）交互

```python
# 監測數據流
Ch3 → Kafka Topic: source_health_events
Ch9 → 消費事件 → 觸發告警

# 統計数據
GET /api/v1/monitoring/health-report
GET /api/v1/monitoring/metrics/summary
```

#### 9.9.3 與Ch4（工作流引擎）交互

```python
# 工作流執行
POST /api/v1/workflows/execute
GET /api/v1/workflows/{id}/status

# 通過消息隊列
Ch4 → Kafka: workflow.events
Ch9 → 監控工作流狀態
```

#### 9.9.4 與Ch8（分布式爬蟲）交互

```python
# 任務調度
POST /api/v1/tasks/create           # 創建爬蟲任務
GET /api/v1/tasks/{id}/logs         # 獲取任務日誌
POST /api/v1/tasks/{id}/cancel      # 取消任務

# 資源管理
GET /api/v1/resources/available      # 查詢可用資源
POST /api/v1/resources/request       # 申請資源
```

#### 9.9.5 與Ch10（實時數據處理）交互

```python
# 流式數據
Ch8 → Ch10 → Ch9 (監控實時指標)

# 事件流
Kafka Topics:
  - crawler.events
  - processing.events
  - quality.events
```

#### 9.9.6 與Ch11（調度與資源管理）交互

```python
# 資源分配
POST /api/v1/scheduler/allocate
GET /api/v1/scheduler/status
PUT /api/v1/scheduler/scale

# 依賴關係
Ch9 ← Ch11 (統一調度平台)
```

#### 9.9.7 與Ch12（數據質量管理）交互

```python
# 質量監控
GET /api/v1/quality/score
GET /api/v1/quality/report

# 質量告警
Event: quality.alert.triggered
Event: quality.issue.detected
```

#### 9.9.8 整體交互流程

```
用戶請求
  ↓
Ch9 (系統整合與部署)
  ├→ Ch1 (資料源管理)
  ├→ Ch3 (健康監測)
  ├→ Ch4 (工作流)
  ├→ Ch8 (爬蟲執行)
  ├→ Ch10 (實時處理)
  ├→ Ch11 (資源調度)
  └→ Ch12 (質量保障)
  ↓
結果返回給用戶
```

---

## 📑 相關章節

|| 前序 | 當前 | 後續 |
||-----|------|------|
|| [9.8](ch9-8-安全考慮.md) | **9.9** | [9.10](ch9-10-最佳實踐指南.md) |

**快速鏈接：**
- [← 返回第9章首頁](ch9-index.md)
